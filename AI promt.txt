Back testing 'Mean Reversion Strategy’

Steps involved :-
1. Buy previous day's biggest losers in Nifty50 index at adjusted closing price

2. Hold for 1 trading day

3. Sell at today's adjusted closing price


AI Prompt:
step1:- Retrieving Nifty50 Constituents and Price Data

Write a python code to perform the following: Fetch the current nifty50 constituents (and their current weights in index if available) from Wikipedia or official exchange portal and save them to a CSV file. Fetch 3 years of daily adjusted close prices for each constituent using necessary python module/package and save the data to a separate CSV file. Handle any errors and print the stock symbol that caused the error. Load the constituent symbols and price data from the CSV files into a DataFrame, and handle missing data with care making sure it will not cause any problem use any module/pakage if necessary.

step2:-Implementing the 'Mean Reversion Strategy'

1. Calculate Daily Returns
Write Python code to calculate the daily returns (based on the adjusted close prices) for each stock in the DataFrame containing the nifty50 prices.

2. Identify Biggest Losers
Write Python code to identify the 10 stocks with the lowest returns for each trading day in the DataFrame containing daily returns. Skip the very first day because that contains only no values.

3. Simulate Trades
Code a trading simulation using the DataFrame holding the 10 lowest daily returning stocks:
1.Ensure dates are in the correct datetime format (use any necessary pakage if needed)
2.For each date, select the 10 worst performers
3.Calculate next day's prices using merge with original data, not shift
4.Start with 100000 rupees, divide equally among 10 stocks daily
5.Buy at day's adjusted close, sell at next day's adjusted close
6.Update capital daily
7.Store results in DataFrame with 'Date' and 'Capital' columns(make a goodlooking table using any proper pakages/modules)
8.Skip trades if insufficient capital or missing price data
9.Handle end of dataset carefully Implement robust error checking. Ensure capital isn't erroneously depleted or inflated.
10.print each day's 'average and median standard deviation','average return' and ' average sharp ratio'- using the  mean standard deviation and assuming risk free rate to be 0.
10.Print final capital and total return Using adjusted close prices and CAGR of those 3 years

Compare with Nifty50 Index:-

Computing Nifty50 returns & metrics:
Fetch the daily adjusted close prices for the NIFTYIETF.NS ETF from Yahoo Finance for the same period as the NIfty50 data.(use the csv file containing nifty50 prices to track the exact dates and period)
Calculate the daily returns for NIFTYIETF.NS.

Write Python code to print the mean reversion strategy’s CAGR, annual standard deviation, and Sharpe ratio side by side with the NIFTYIETF.NS. Nicely display which strategy had the higher risk-adjusted returns as measured by the Sharpe ratio (assume a risk-free rate of 0).Compare the Sharpe ratio of our mean reversion strategy with that of NIFTYIETF.NS. Print a message indicating whether our strategy outperformed the general NIfty50 based on the Sharpe ratios.

Visualize Portfolio Growth:-
1.Calculate the cumulative returns for both our mean reversion strategy and the NIFTYIETF.NS ETF.
2.Multiply the cumulative returns by the initial investment of 100,000 rupees to get the daily portfolio values for both strategies.
3.Create a beautiful interactive plot using best module/package for chart to visualize the growth of the 100,000 rupees portfolio over time for both our mean reversion strategy and the NIFTYIETF.NS ETF.
4.Include proper axis labels, a title, and a legend.
Disp